import React, { useState } from 'react'
import * as data from './film-data.json'

function filmInfo() {
    // const movieInfo = [
    //     { film.map(film, index) }
    // ]
    return (
        console.log("hello")
    )
}

export default filmInfo